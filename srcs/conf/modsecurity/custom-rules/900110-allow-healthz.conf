# 900110 - Allow health checks without CRS processing (e.g., /healthz, /ping)
SecRule REQUEST_URI "@beginsWith /healthz" "id:900110,phase:1,pass,ctl:ruleEngine=Off,nolog"
SecRule REQUEST_URI "@beginsWith /ping"    "id:900111,phase:1,pass,ctl:ruleEngine=Off,nolog"
