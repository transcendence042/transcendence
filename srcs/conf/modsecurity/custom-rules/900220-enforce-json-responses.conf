# /api responses must be JSON (adjust if returning other types)
SecRule REQUEST_URI "@beginsWith /api/" \
    "id:900220,phase:4,deny,status:500,log,msg:'Non-JSON response in /api',chain"
    SecRule RESPONSE_HEADERS:Content-Type "!@rx ^application/json\b"
