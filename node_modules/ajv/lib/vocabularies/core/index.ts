import type {Vocabulary} from "ajv/lib/types"
import idKeyword from "ajv/lib/vocabularies/core/id"
import refKeyword from "ajv/lib/vocabularies/core/ref"

const core: Vocabulary = [
  "$schema",
  "$id",
  "$defs",
  "$vocabulary",
  {keyword: "$comment"},
  "definitions",
  idKeyword,
  refKeyword,
]

export default core
