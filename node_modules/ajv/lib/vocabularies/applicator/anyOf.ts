import type {CodeKeywordDefinition, ErrorNoParams, AnySchema} from "ajv/lib/types"
import {validateUnion} from "ajv/lib/vocabularies/code"

export type AnyOfError = ErrorNoParams<"anyOf", AnySchema[]>

const def: CodeKeywordDefinition = {
  keyword: "anyOf",
  schemaType: "array",
  trackErrors: true,
  code: validateUnion,
  error: {message: "must match a schema in anyOf"},
}

export default def
